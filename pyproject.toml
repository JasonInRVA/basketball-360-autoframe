[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "basketball-360-autoframe"
version = "0.3.0"
description = "Generate Insta360 Studio sidecar files to auto-reframe 360° basketball video using behavioral cloning"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Jason Sewell" },
]
keywords = ["basketball", "360-video", "auto-reframe", "insta360", "behavioral-cloning"]
dependencies = [
    "torch>=2.0",
    "torchvision>=0.15",
    "opencv-python>=4.8",
    "numpy>=1.24",
    "scipy>=1.11",
    "typer[all]>=0.12",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
]

[project.scripts]
autoframe = "autoframe.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/autoframe"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = ["-ra", "--strict-markers", "-x", "--tb=short"]
markers = [
    "slow: tests that process video or run model inference",
    "integration: tests requiring GPU",
]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM"]

[tool.ruff.lint.isort]
known-first-party = ["autoframe"]
